/*
export function setUserData (state, token) {
    const userData = {} // decode(token).userData

    state.token = token
    state.email = userData.email
    state.firstName = userData.first_name
    state.lastName = userData.last_name
  }
  */